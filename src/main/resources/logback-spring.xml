<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<property name="logFileName" value="demo-rest-app" />
	<property name="logFileNameDebug" value="demo-rest-app-debug" />

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <layout class="ch.qos.logback.classic.PatternLayout">
			<Pattern>[%d{dd-MM-yyyy HH:mm:ss.SSS}] [%thread] [%-5level] %logger{70} %M \(%L\) - %msg%n</Pattern>
        </layout>
    </appender>

    <appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logPath}/${logFileName}.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<Pattern>[%d{dd-MM-yyyy HH:mm:ss.SSS}] [%thread] [%-5level] %logger{70} %M \(%L\) - %msg%n</Pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- rollover daily and when the file reaches 10 MegaBytes -->
            <fileNamePattern>${logPath}/archived/${logFileName}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFileNamingAndTriggeringPolicy">
                <maxFileSize>100KB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
    </appender>
    
    
    <appender name="RollingFileDebug" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${logPath}/${logFileNameDebug}.log</file>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<Pattern>[%d{dd-MM-yyyy HH:mm:ss.SSS}] [%thread] [%-5level] %logger{70} %M \(%L\) - %msg%n</Pattern>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- rollover daily and when the file reaches 100KB MegaBytes -->
            <fileNamePattern>${logPath}/archived/${logFileNameDebug}-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFileNamingAndTriggeringPolicy">
                <maxFileSize>100KB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>
    </appender>

	<springProfile name="dev">
		<property resource="application-dev.properties" />
        <!-- LOG everything at ERROR level -->
		<root level="error">
			<appender-ref ref="Console" />
		</root>
		<!-- LOG "it.technologydata*" at DEBUG level -->
		<logger name="it.technologydata" level="debug" additivity="false">
			<appender-ref ref="Console" />
		</logger>
		<!--
		<logger name="org.springframework.security.web.DefaultSecurityFilterChain" level="debug" additivity="false">
			<appender-ref ref="Console" />
		</logger>
		<logger name="org.springframework.security.web.FilterChainProxy" level="debug" additivity="false">
			<appender-ref ref="Console" />
		</logger>
		-->        
    </springProfile>
    <springProfile name="cert">
		<property resource="application-cert.properties" />
		<property name="logPath" value="${logging.file.path}" />
		<!-- LOG everything at ERROR level -->
		<root level="error">
			<appender-ref ref="RollingFileDebug" />
			<appender-ref ref="Console" />
		</root>
		<!-- LOG "it.technologydata*" at DEBUG level -->
		<logger name="it.technologydata" level="debug" additivity="false">
			<appender-ref ref="RollingFileDebug" />
			<appender-ref ref="Console" />
		</logger>
		
		<logger name="org.springframework.security.web.DefaultSecurityFilterChain" level="debug" additivity="false">
			<appender-ref ref="Console" />
		</logger>
		<logger name="org.springframework.security.web.FilterChainProxy" level="debug" additivity="false">
			<appender-ref ref="Console" />
		</logger>
    </springProfile>
	<springProfile name="prod">
		<property resource="application-prod.properties" />
		<property name="logPath" value="${logging.file.path}" />
		<!-- LOG everything at ERROR level -->
		<root level="error">
			<appender-ref ref="RollingFile" />
		</root>
        <!-- LOG "it.technologydata*" at INFO level -->
		<logger name="it.technologydata" level="info" additivity="false">
			<appender-ref ref="RollingFile" />
		</logger>
    </springProfile>
</configuration>
